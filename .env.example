STELLAR_NETWORK=testnet
STELLAR_RPC_URL=https://soroban-testnet.stellar.org
HORIZON_URL=https://horizon-testnet.stellar.org
FRIENDBOT_URL=https://friendbot.stellar.org

FLOWLOCK_CONTRACT_ID=
USDC_CONTRACT_ID=
XLM_CONTRACT_ID=

SOROSWAP_API_URL=https://api.soroswap.finance

# Keypair names (generate with: stellar keys generate <name> --network testnet --fund)
# alice = payer
# bob = provider
# platform = platform fee recipient
# keeper-bot = keeper automation
